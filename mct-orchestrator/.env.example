MESH_CONTROLLER_URL=https://mesh.example.internal/api/v1
MESH_AUTH_TOKEN=replace-with-real-secret
MESH_REQUEST_TIMEOUT=10

# Hotspot/backoffice trust assertions are intentionally not configured as static
# environment flags. In production they must come from a trusted tunnel/session
# identity source rather than caller-controlled configuration.
